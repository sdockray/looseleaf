books without covers

## installation

```sh
git submodule init
git submodule update

sudo apt-get install ghostscript python-twisted-web python-couchdb
(or)
# brew install ghostscript
brew install poppler
pip install -r requirements.txt
```

## usage

python looseleaf.py DATABASE_DIR
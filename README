books without covers

## installation

```sh
git submodule init
git submodule update

sudo apt-get install ghostscript python-twisted-web python-couchdb
```

## usage

python looseleaf.py DATABASE_DIR [FIRST_RUN_IMPORT_DIR]